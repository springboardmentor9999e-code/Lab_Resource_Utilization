name: Build & Push Docker Images to GHCR

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: badalsingh25/lab-resource-platform
<<<<<<< HEAD
=======

>>>>>>> db5ed35 (Fix GHCR lowercase repository name)
jobs:
  build-and-push:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and Push Backend Image
        uses: docker/build-push-action@v6
        with:
          context: ./backend
          push: true
          tags: |
            ghcr.io/badalsingh25/lab-resource-platform-backend:latest
            ghcr.io/badalsingh25/lab-resource-platform-backend:${{ github.sha }}

      - name: Build and Push Frontend Image
        uses: docker/build-push-action@v6
        with:
          context: ./frontend
          push: true
          tags: |
            ghcr.io/badalsingh25/lab-resource-platform-frontend:latest
            ghcr.io/badalsingh25/lab-resource-platform-frontend:${{ github.sha }}