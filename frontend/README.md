# Lab Resource Utilization Platform - Frontend

React frontend for the Lab Resource Utilization Platform.

## Setup

```bash
npm install
npm run dev
```

The app expects the backend API base URL at `http://localhost:8080/api` by default. Override it with:

```bash
VITE_API_BASE_URL=http://localhost:8080/api
```
